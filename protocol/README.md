# protocol

Definitions for the runner-orchestrator protocol (lease, heartbeat, complete,
and related messages). Structures here must stay aligned with
`docs/reference/runner-messages.md` and avoid embedding secrets.
