# internal

Packages shared across binaries that are not part of the public API surface.
Use this directory for helpers such as configuration loading, logging setup,
and shared utilities that should not be imported by external consumers.
